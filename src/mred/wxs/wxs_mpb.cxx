/* DO NOT EDIT THIS FILE. */
/* This file was generated by xctocc from "wxs_mpb.xc". */


#if defined(_MSC_VER)
# include "wx.h"
#endif

#include "wx_media.h"





#include "wxscheme.h"
#include "wxs_mpb.h"
#include "wxscomon.h"






/* 
   X = Not meant for virtual, defined in wxMediaEdit/wxMediaPasteboard
       and overloaded there
   Y = Virtual, defined in wxMediaEdit/wxMediaPasteboard
   Z = Virtual, defined in wxMediaBuffer 
*/





























  












class os_wxMediaPasteboard : public wxMediaPasteboard {
 public:

  os_wxMediaPasteboard(Scheme_Object * obj);
  ~os_wxMediaPasteboard();
  void AfterInteractiveResize(class wxSnip* x0);
  Bool OnInteractiveResize(class wxSnip* x0);
  void AfterInteractiveMove();
  Bool OnInteractiveMove();
  void InteractiveAdjustResize(class wxSnip* x0, float* x1, float* x2);
  void InteractiveAdjustMove(class wxSnip* x0, float* x1, float* x2);
  void InteractiveAdjustMouse(float* x0, float* x1);
  void OnDoubleClick(class wxSnip* x0, class wxMouseEvent& x1);
  void AfterSelect(class wxSnip* x0, Bool x1);
  Bool OnSelect(class wxSnip* x0, Bool x1);
  void AfterResize(class wxSnip* x0, float x1, float x2, Bool x3);
  Bool OnResize(class wxSnip* x0, float x1, float x2);
  void AfterMoveTo(class wxSnip* x0, float x1, float x2, Bool x3);
  Bool OnMoveTo(class wxSnip* x0, float x1, float x2, Bool x3);
  void AfterDelete(class wxSnip* x0);
  Bool OnDelete(class wxSnip* x0);
  void AfterInsert(class wxSnip* x0, class wxSnip* x1, float x2, float x3);
  Bool OnInsert(class wxSnip* x0, class wxSnip* x1, float x2, float x3);
  void DoPaste(long x0);
  void DoCopy(long x0, Bool x1);
  nstring PutFile(nstring x0, nstring x1);
  nstring GetFile(nstring x0);
  void AfterEditSequence();
  void OnEditSequence();
  void AfterLoadFile(Bool x0);
  Bool OnLoadFile(string x0, int x1);
  void AfterSaveFile(Bool x0);
  Bool OnSaveFile(string x0, int x1);
  class wxSnip* OnNewBox(int x0);
  class wxImageSnip* OnNewImageSnip(nstring x0, long x1, Bool x2, Bool x3);
  void InvalidateBitmapCache(float x0 = 0.0, float x1 = 0.0, float x2 = -1.0, float x3 = -1.0);
  void OnPaint(Bool x0, class wxDC* x1, float x2, float x3, float x4, float x5, float x6, float x7, int x8);
  Bool WriteFootersToFile(class wxMediaStreamOut& x0);
  Bool WriteHeadersToFile(class wxMediaStreamOut& x0);
  Bool ReadFooterFromFile(class wxMediaStreamIn& x0, string x1);
  Bool ReadHeaderFromFile(class wxMediaStreamIn& x0, string x1);
  void SetFilename(string x0, Bool x1 = FALSE);
  Bool ReleaseSnip(class wxSnip* x0);
  void SetModified(Bool x0);
  void SetSnipData(class wxSnip* x0, class wxBufferData* x1);
  class wxBufferData* GetSnipData(class wxSnip* x0);
  void NeedsUpdate(class wxSnip* x0, float x1, float x2, float x3, float x4);
  void Resized(class wxSnip* x0, Bool x1);
  void SetCaretOwner(class wxSnip* x0, int x1 = wxFOCUS_IMMEDIATE);
  Bool ScrollTo(class wxSnip* x0, float x1, float x2, float x3, float x4, Bool x5);
  void OnChange();
  void OnFocus(Bool x0);
  void OnDefaultChar(class wxKeyEvent& x0);
  void OnDefaultEvent(class wxMouseEvent& x0);
  void OnLocalChar(class wxKeyEvent& x0);
  void OnLocalEvent(class wxMouseEvent& x0);
  void SizeCacheInvalid();
  void OwnCaret(Bool x0);
  void Refresh(float x0, float x1, float x2, float x3, Bool x4);
  class wxCursor* AdjustCursor(class wxMouseEvent& x0);
  void OnChar(class wxKeyEvent& x0);
  void OnEvent(class wxMouseEvent& x0);
};

Scheme_Object *os_wxMediaPasteboard_class;

os_wxMediaPasteboard::os_wxMediaPasteboard(Scheme_Object * o)
: wxMediaPasteboard()
{
  __gc_external = (void *)o;
  objscheme_backpointer(&__gc_external);
  objscheme_note_creation(o);
}

os_wxMediaPasteboard::~os_wxMediaPasteboard()
{
    objscheme_destroy(this, (Scheme_Object *)__gc_external);
}

void os_wxMediaPasteboard::AfterInteractiveResize(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-interactive-resize", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterInteractiveResize(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnInteractiveResize(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-interactive-resize", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnInteractiveResize(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-interactive-resize"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterInteractiveMove()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-interactive-move", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterInteractiveMove();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnInteractiveMove()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-interactive-move", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnInteractiveMove();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-interactive-move"", extracting return value");
  }
}

void os_wxMediaPasteboard::InteractiveAdjustResize(class wxSnip* x0, float* x1, float* x2)
{
  Scheme_Object *p[3];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "interactive-adjust-resize", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::InteractiveAdjustResize(x0, x1, x2);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_box(scheme_make_double((*x1)));
  p[2] = objscheme_box(scheme_make_double((*x2)));
  

  v = scheme_apply(method, 3, p);
  
  if (x1) *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-resize"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-resize"", extracting return value via box");
  if (x2) *x2 = objscheme_unbundle_float(objscheme_unbox(p[2], "wx:media-pasteboard%::interactive-adjust-resize"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-resize"", extracting return value via box");
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::InteractiveAdjustMove(class wxSnip* x0, float* x1, float* x2)
{
  Scheme_Object *p[3];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "interactive-adjust-move", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::InteractiveAdjustMove(x0, x1, x2);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_box(scheme_make_double((*x1)));
  p[2] = objscheme_box(scheme_make_double((*x2)));
  

  v = scheme_apply(method, 3, p);
  
  if (x1) *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-move"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-move"", extracting return value via box");
  if (x2) *x2 = objscheme_unbundle_float(objscheme_unbox(p[2], "wx:media-pasteboard%::interactive-adjust-move"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-move"", extracting return value via box");
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::InteractiveAdjustMouse(float* x0, float* x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "interactive-adjust-mouse", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::InteractiveAdjustMouse(x0, x1);
  } else {
  
  p[0] = objscheme_box(scheme_make_double((*x0)));
  p[1] = objscheme_box(scheme_make_double((*x1)));
  

  v = scheme_apply(method, 2, p);
  
  if (x0) *x0 = objscheme_unbundle_float(objscheme_unbox(p[0], "wx:media-pasteboard%::interactive-adjust-mouse"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-mouse"", extracting return value via box");
  if (x1) *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-mouse"", extracting return value via box"), "wx:media-pasteboard%::interactive-adjust-mouse"", extracting return value via box");
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnDoubleClick(class wxSnip* x0, class wxMouseEvent& x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-double-click", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnDoubleClick(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_bundle_wxMouseEvent(&x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::AfterSelect(class wxSnip* x0, Bool x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-select", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterSelect(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = (x1 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnSelect(class wxSnip* x0, Bool x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-select", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnSelect(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = (x1 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-select"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterResize(class wxSnip* x0, float x1, float x2, Bool x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-resize", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterResize(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = (x3 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnResize(class wxSnip* x0, float x1, float x2)
{
  Scheme_Object *p[3];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-resize", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnResize(x0, x1, x2);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  

  v = scheme_apply(method, 3, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-resize"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterMoveTo(class wxSnip* x0, float x1, float x2, Bool x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-move-to", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterMoveTo(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = (x3 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnMoveTo(class wxSnip* x0, float x1, float x2, Bool x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-move-to", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnMoveTo(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = (x3 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-move-to"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterDelete(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-delete", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterDelete(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnDelete(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-delete", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnDelete(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-delete"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterInsert(class wxSnip* x0, class wxSnip* x1, float x2, float x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-insert", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterInsert(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_bundle_wxSnip(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnInsert(class wxSnip* x0, class wxSnip* x1, float x2, float x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-insert", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnInsert(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_bundle_wxSnip(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-insert"", extracting return value");
  }
}

void os_wxMediaPasteboard::DoPaste(long x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "do-paste", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::DoPaste(x0);
  } else {
  
  p[0] = scheme_make_integer(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::DoCopy(long x0, Bool x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "do-copy", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::DoCopy(x0, x1);
  } else {
  
  p[0] = scheme_make_integer(x0);
  p[1] = (x1 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

nstring os_wxMediaPasteboard::PutFile(nstring x0, nstring x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "put-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::PutFile(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  p[1] = objscheme_bundle_string((char *)x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return (nstring)objscheme_unbundle_nullable_string(v, "wx:media-pasteboard%::put-file"", extracting return value");
  }
}

nstring os_wxMediaPasteboard::GetFile(nstring x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "get-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::GetFile(x0);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return (nstring)objscheme_unbundle_nullable_string(v, "wx:media-pasteboard%::get-file"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterEditSequence()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-edit-sequence", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterEditSequence();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnEditSequence()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-edit-sequence", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnEditSequence();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::AfterLoadFile(Bool x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-load-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterLoadFile(x0);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnLoadFile(string x0, int x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-load-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnLoadFile(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  p[1] = scheme_make_integer(x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-load-file"", extracting return value");
  }
}

void os_wxMediaPasteboard::AfterSaveFile(Bool x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "after-save-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::AfterSaveFile(x0);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::OnSaveFile(string x0, int x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-save-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnSaveFile(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  p[1] = scheme_make_integer(x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::on-save-file"", extracting return value");
  }
}

class wxSnip* os_wxMediaPasteboard::OnNewBox(int x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-new-box", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnNewBox(x0);
  } else {
  
  p[0] = scheme_make_integer(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_wxSnip(v, "wx:media-pasteboard%::on-new-box"", extracting return value", 0);
  }
}

class wxImageSnip* os_wxMediaPasteboard::OnNewImageSnip(nstring x0, long x1, Bool x2, Bool x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-new-image-snip", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::OnNewImageSnip(x0, x1, x2, x3);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  p[1] = scheme_make_integer(x1);
  p[2] = (x2 ? scheme_true : scheme_false);
  p[3] = (x3 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_wxImageSnip(v, "wx:media-pasteboard%::on-new-image-snip"", extracting return value", 0);
  }
}

void os_wxMediaPasteboard::InvalidateBitmapCache(float x0, float x1, float x2, float x3)
{
  Scheme_Object *p[4];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "invalidate-bitmap-cache", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::InvalidateBitmapCache(x0, x1, x2, x3);
  } else {
  
  p[0] = scheme_make_double(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  

  v = scheme_apply(method, 4, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnPaint(Bool x0, class wxDC* x1, float x2, float x3, float x4, float x5, float x6, float x7, int x8)
{
  Scheme_Object *p[9];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-paint", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnPaint(x0, x1, x2, x3, x4, x5, x6, x7, x8);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  p[1] = objscheme_bundle_wxDC(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  p[4] = scheme_make_double(x4);
  p[5] = scheme_make_double(x5);
  p[6] = scheme_make_double(x6);
  p[7] = scheme_make_double(x7);
  p[8] = scheme_make_integer(x8);
  

  v = scheme_apply(method, 9, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::WriteFootersToFile(class wxMediaStreamOut& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "write-footers-to-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::WriteFootersToFile(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMediaStreamOut(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::write-footers-to-file"", extracting return value");
  }
}

Bool os_wxMediaPasteboard::WriteHeadersToFile(class wxMediaStreamOut& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "write-headers-to-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::WriteHeadersToFile(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMediaStreamOut(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::write-headers-to-file"", extracting return value");
  }
}

Bool os_wxMediaPasteboard::ReadFooterFromFile(class wxMediaStreamIn& x0, string x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "read-footer-from-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::ReadFooterFromFile(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxMediaStreamIn(&x0);
  p[1] = objscheme_bundle_string((char *)x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::read-footer-from-file"", extracting return value");
  }
}

Bool os_wxMediaPasteboard::ReadHeaderFromFile(class wxMediaStreamIn& x0, string x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "read-header-from-file", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::ReadHeaderFromFile(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxMediaStreamIn(&x0);
  p[1] = objscheme_bundle_string((char *)x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::read-header-from-file"", extracting return value");
  }
}

void os_wxMediaPasteboard::SetFilename(string x0, Bool x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "set-filename", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::SetFilename(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_string((char *)x0);
  p[1] = (x1 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::ReleaseSnip(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "release-snip", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::ReleaseSnip(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::release-snip"", extracting return value");
  }
}

void os_wxMediaPasteboard::SetModified(Bool x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "set-modified", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::SetModified(x0);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::SetSnipData(class wxSnip* x0, class wxBufferData* x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "set-snip-data", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::SetSnipData(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = objscheme_bundle_wxBufferData(x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

class wxBufferData* os_wxMediaPasteboard::GetSnipData(class wxSnip* x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "get-snip-data", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::GetSnipData(x0);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_wxBufferData(v, "wx:media-pasteboard%::get-snip-data"", extracting return value", 1);
  }
}

void os_wxMediaPasteboard::NeedsUpdate(class wxSnip* x0, float x1, float x2, float x3, float x4)
{
  Scheme_Object *p[5];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "needs-update", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::NeedsUpdate(x0, x1, x2, x3, x4);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  p[4] = scheme_make_double(x4);
  

  v = scheme_apply(method, 5, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::Resized(class wxSnip* x0, Bool x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "resized", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::Resized(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = (x1 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::SetCaretOwner(class wxSnip* x0, int x1)
{
  Scheme_Object *p[2];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "set-caret-owner", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::SetCaretOwner(x0, x1);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_integer(x1);
  

  v = scheme_apply(method, 2, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

Bool os_wxMediaPasteboard::ScrollTo(class wxSnip* x0, float x1, float x2, float x3, float x4, Bool x5)
{
  Scheme_Object *p[6];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "scroll-to", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::ScrollTo(x0, x1, x2, x3, x4, x5);
  } else {
  
  p[0] = objscheme_bundle_wxSnip(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  p[4] = scheme_make_double(x4);
  p[5] = (x5 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 6, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_bool(v, "wx:media-pasteboard%::scroll-to"", extracting return value");
  }
}

void os_wxMediaPasteboard::OnChange()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-change", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnChange();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnFocus(Bool x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-focus", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnFocus(x0);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnDefaultChar(class wxKeyEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-default-char", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnDefaultChar(x0);
  } else {
  
  p[0] = objscheme_bundle_wxKeyEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnDefaultEvent(class wxMouseEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-default-event", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnDefaultEvent(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMouseEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnLocalChar(class wxKeyEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-local-char", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnLocalChar(x0);
  } else {
  
  p[0] = objscheme_bundle_wxKeyEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnLocalEvent(class wxMouseEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-local-event", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnLocalEvent(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMouseEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::SizeCacheInvalid()
{
  Scheme_Object **p = NULL;
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "size-cache-invalid", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::SizeCacheInvalid();
  } else {
  
  

  v = scheme_apply(method, 0, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OwnCaret(Bool x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "own-caret", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OwnCaret(x0);
  } else {
  
  p[0] = (x0 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::Refresh(float x0, float x1, float x2, float x3, Bool x4)
{
  Scheme_Object *p[5];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "refresh", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::Refresh(x0, x1, x2, x3, x4);
  } else {
  
  p[0] = scheme_make_double(x0);
  p[1] = scheme_make_double(x1);
  p[2] = scheme_make_double(x2);
  p[3] = scheme_make_double(x3);
  p[4] = (x4 ? scheme_true : scheme_false);
  

  v = scheme_apply(method, 5, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

class wxCursor* os_wxMediaPasteboard::AdjustCursor(class wxMouseEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "adjust-cursor", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    return wxMediaPasteboard::AdjustCursor(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMouseEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  return objscheme_unbundle_wxCursor(v, "wx:media-pasteboard%::adjust-cursor"", extracting return value", 1);
  }
}

void os_wxMediaPasteboard::OnChar(class wxKeyEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-char", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnChar(x0);
  } else {
  
  p[0] = objscheme_bundle_wxKeyEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

void os_wxMediaPasteboard::OnEvent(class wxMouseEvent& x0)
{
  Scheme_Object *p[1];
  Scheme_Object *v;
  jmp_buf savebuf;
  Scheme_Object *method;
  int sj;
  static void *mcache = 0;

  method = objscheme_find_method((Scheme_Object *)__gc_external, os_wxMediaPasteboard_class, "on-event", &mcache);
  if (method && !OBJSCHEME_PRIM_METHOD(method)) {
    COPY_JMPBUF(savebuf, scheme_error_buf);
    sj = scheme_setjmp(scheme_error_buf);
  } else sj = 1;
  if (sj) {
    if (method && !OBJSCHEME_PRIM_METHOD(method))
      COPY_JMPBUF(scheme_error_buf, savebuf);
    wxMediaPasteboard::OnEvent(x0);
  } else {
  
  p[0] = objscheme_bundle_wxMouseEvent(&x0);
  

  v = scheme_apply(method, 1, p);
  
  
  COPY_JMPBUF(scheme_error_buf, savebuf);

  }
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetScrollStep(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  float x0;

  
  x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::set-scroll-step");

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetScrollStep(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetScrollStep(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  float r;
  objscheme_check_valid(obj);

  

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetScrollStep();

  
  
  return scheme_make_double(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetSelectionVisible(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::set-selection-visible");

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetSelectionVisible(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetSelectionVisible(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);

  

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetSelectionVisible();

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetDragable(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::set-dragable");

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetDragable(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetDragable(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);

  

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetDragable();

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterInteractiveResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-interactive-resize", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterInteractiveResize(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterInteractiveResize(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnInteractiveResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-interactive-resize", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnInteractiveResize(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnInteractiveResize(x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterInteractiveMove(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterInteractiveMove();
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterInteractiveMove();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnInteractiveMove(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnInteractiveMove();
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnInteractiveMove();

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardInteractiveAdjustResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float _x1;
  float* x1 = &_x1;
  float _x2;
  float* x2 = &_x2;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::interactive-adjust-resize", 0);
  if (SCHEME_NULLP(p[1]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-resize", "non-null", (1 - 0), n, p);
  else
    *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-resize"), "wx:media-pasteboard%::interactive-adjust-resize");
  if (SCHEME_NULLP(p[2]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-resize", "non-null", (2 - 0), n, p);
  else
    *x2 = objscheme_unbundle_float(objscheme_unbox(p[2], "wx:media-pasteboard%::interactive-adjust-resize"), "wx:media-pasteboard%::interactive-adjust-resize");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::InteractiveAdjustResize(x0, x1, x2);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->InteractiveAdjustResize(x0, x1, x2);

  
  if (n > 1)
    objscheme_set_box(p[1], scheme_make_double(_x1));
  if (n > 2)
    objscheme_set_box(p[2], scheme_make_double(_x2));
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardInteractiveAdjustMove(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float _x1;
  float* x1 = &_x1;
  float _x2;
  float* x2 = &_x2;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::interactive-adjust-move", 0);
  if (SCHEME_NULLP(p[1]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-move", "non-null", (1 - 0), n, p);
  else
    *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-move"), "wx:media-pasteboard%::interactive-adjust-move");
  if (SCHEME_NULLP(p[2]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-move", "non-null", (2 - 0), n, p);
  else
    *x2 = objscheme_unbundle_float(objscheme_unbox(p[2], "wx:media-pasteboard%::interactive-adjust-move"), "wx:media-pasteboard%::interactive-adjust-move");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::InteractiveAdjustMove(x0, x1, x2);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->InteractiveAdjustMove(x0, x1, x2);

  
  if (n > 1)
    objscheme_set_box(p[1], scheme_make_double(_x1));
  if (n > 2)
    objscheme_set_box(p[2], scheme_make_double(_x2));
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardInteractiveAdjustMouse(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  float _x0;
  float* x0 = &_x0;
  float _x1;
  float* x1 = &_x1;

  
  if (SCHEME_NULLP(p[0]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-mouse", "non-null", (0 - 0), n, p);
  else
    *x0 = objscheme_unbundle_float(objscheme_unbox(p[0], "wx:media-pasteboard%::interactive-adjust-mouse"), "wx:media-pasteboard%::interactive-adjust-mouse");
  if (SCHEME_NULLP(p[1]))
    scheme_wrong_type("wx:media-pasteboard%::interactive-adjust-mouse", "non-null", (1 - 0), n, p);
  else
    *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::interactive-adjust-mouse"), "wx:media-pasteboard%::interactive-adjust-mouse");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::InteractiveAdjustMouse(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->InteractiveAdjustMouse(x0, x1);

  
  if (n > 0)
    objscheme_set_box(p[0], scheme_make_double(_x0));
  if (n > 1)
    objscheme_set_box(p[1], scheme_make_double(_x1));
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnDoubleClick(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxMouseEvent* x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-double-click", 0);
  x1 = objscheme_unbundle_wxMouseEvent(p[1], "wx:media-pasteboard%::on-double-click", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnDoubleClick(x0, *x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnDoubleClick(x0, *x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterSelect(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  Bool x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-select", 0);
  x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::after-select");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterSelect(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterSelect(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnSelect(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  Bool x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-select", 0);
  x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::on-select");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnSelect(x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnSelect(x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;
  Bool x3;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-resize", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::after-resize");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::after-resize");
  x3 = objscheme_unbundle_bool(p[3], "wx:media-pasteboard%::after-resize");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterResize(x0, x1, x2, x3);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterResize(x0, x1, x2, x3);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-resize", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::on-resize");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::on-resize");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnResize(x0, x1, x2);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnResize(x0, x1, x2);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterMoveTo(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;
  Bool x3;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-move-to", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::after-move-to");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::after-move-to");
  x3 = objscheme_unbundle_bool(p[3], "wx:media-pasteboard%::after-move-to");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterMoveTo(x0, x1, x2, x3);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterMoveTo(x0, x1, x2, x3);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnMoveTo(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;
  Bool x3;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-move-to", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::on-move-to");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::on-move-to");
  x3 = objscheme_unbundle_bool(p[3], "wx:media-pasteboard%::on-move-to");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnMoveTo(x0, x1, x2, x3);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnMoveTo(x0, x1, x2, x3);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterDelete(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-delete", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterDelete(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterDelete(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnDelete(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-delete", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnDelete(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnDelete(x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterInsert(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxSnip* x1;
  float x2;
  float x3;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::after-insert", 0);
  x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::after-insert", 1);
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::after-insert");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::after-insert");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterInsert(x0, x1, x2, x3);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterInsert(x0, x1, x2, x3);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnInsert(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxSnip* x1;
  float x2;
  float x3;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::on-insert", 0);
  x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::on-insert", 1);
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::on-insert");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::on-insert");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnInsert(x0, x1, x2, x3);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnInsert(x0, x1, x2, x3);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardFindNextSelectedSnip(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxSnip* r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::find-next-selected-snip", 1);

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->FindNextSelectedSnip(x0);

  
  
  return objscheme_bundle_wxSnip(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardIsSelected(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::is-selected?", 1);

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->IsSelected(x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardFindFirstSnip(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxSnip* r;
  objscheme_check_valid(obj);

  

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->FindFirstSnip();

  
  
  return objscheme_bundle_wxSnip(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardFindSnip(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxSnip* r;
  objscheme_check_valid(obj);
  float x0;
  float x1;

  
  x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::find-snip");
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::find-snip");

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->FindSnip(x0, x1);

  
  
  return objscheme_bundle_wxSnip(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAddPasteboardFunctions(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxKeymap* x0;

  
  x0 = objscheme_unbundle_wxKeymap(p[0], "wx:media-pasteboard%::add-pasteboard-functions", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AddPasteboardFunctions(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetCenter(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  float _x0;
  float* x0 = &_x0;
  float _x1;
  float* x1 = &_x1;

  
  if (SCHEME_NULLP(p[0]))
    scheme_wrong_type("wx:media-pasteboard%::get-center", "non-null", (0 - 0), n, p);
  else
    *x0 = objscheme_unbundle_float(objscheme_unbox(p[0], "wx:media-pasteboard%::get-center"), "wx:media-pasteboard%::get-center");
  if (SCHEME_NULLP(p[1]))
    scheme_wrong_type("wx:media-pasteboard%::get-center", "non-null", (1 - 0), n, p);
  else
    *x1 = objscheme_unbundle_float(objscheme_unbox(p[1], "wx:media-pasteboard%::get-center"), "wx:media-pasteboard%::get-center");

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetCenter(x0, x1);

  
  if (n > 0)
    objscheme_set_box(p[0], scheme_make_double(_x0));
  if (n > 1)
    objscheme_set_box(p[1], scheme_make_double(_x1));
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardRemoveSelected(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::remove-selected", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->RemoveSelected(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardNoSelected(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->NoSelected();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAddSelected(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  if ((n >= 1) && objscheme_istype_wxSnip(p[0], NULL, 0)) {
    class wxSnip* x0;

    
    if (n != 1) 
      scheme_wrong_count("wx:media-pasteboard%::add-selected (wx:snip% case)", 1, 1, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::add-selected (wx:snip% case)", 0);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AddSelected(x0);

    
    
  } else  {
    float x0;
    float x1;
    float x2;
    float x3;

    
    if (n != 4) 
      scheme_wrong_count("wx:media-pasteboard%::add-selected (rectangle case)", 4, 4, n, p);
    x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::add-selected (rectangle case)");
    x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::add-selected (rectangle case)");
    x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::add-selected (rectangle case)");
    x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::add-selected (rectangle case)");

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AddSelected(x0, x1, x2, x3);

    
    
  }

  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetSelected(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::set-selected", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetSelected(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardChangeStyle(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  if ((n >= 2) && objscheme_istype_wxStyleDelta(p[0], NULL, 1) && objscheme_istype_wxSnip(p[1], NULL, 1)) {
    class wxStyleDelta* x0;
    class wxSnip* x1;

    
    if (n != 2) 
      scheme_wrong_count("wx:media-pasteboard%::change-style (wx:style-delta% and wx:snip% case)", 2, 2, n, p);
    x0 = objscheme_unbundle_wxStyleDelta(p[0], "wx:media-pasteboard%::change-style (wx:style-delta% and wx:snip% case)", 1);
    x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::change-style (wx:style-delta% and wx:snip% case)", 1);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ChangeStyle(x0, x1);

    
    
  } else if ((n >= 1) && objscheme_istype_wxStyleDelta(p[0], NULL, 1)) {
    class wxStyleDelta* x0;

    
    if (n != 1) 
      scheme_wrong_count("wx:media-pasteboard%::change-style (wx:style-delta% without position or wx:snip% case)", 1, 1, n, p);
    x0 = objscheme_unbundle_wxStyleDelta(p[0], "wx:media-pasteboard%::change-style (wx:style-delta% without position or wx:snip% case)", 1);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ChangeStyle(x0);

    
    
  } else  {
    class wxStyle* x0;
    class wxSnip* x1;

    
    if ((n < 1) ||(n > 2)) 
      scheme_wrong_count("wx:media-pasteboard%::change-style (wx:style% case)", 1, 2, n, p);
    x0 = objscheme_unbundle_wxStyle(p[0], "wx:media-pasteboard%::change-style (wx:style% case)", 1);
    if (n > 1) {
      x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::change-style (wx:style% case)", 1);
    } else
      x1 = NULL;

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ChangeStyle(x0, x1);

    
    
  }

  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetAfter(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxSnip* x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::set-after", 0);
  x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::set-after", 1);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetAfter(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetBefore(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxSnip* x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::set-before", 0);
  x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::set-before", 1);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetBefore(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardLower(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::lower", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Lower(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardRaise(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::raise", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Raise(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardResize(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::resize", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::resize");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::resize");

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Resize(x0, x1, x2);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardMove(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  if ((n >= 1) && objscheme_istype_wxSnip(p[0], NULL, 0)) {
    class wxSnip* x0;
    float x1;
    float x2;

    
    if (n != 3) 
      scheme_wrong_count("wx:media-pasteboard%::move (wx:snip% case)", 3, 3, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::move (wx:snip% case)", 0);
    x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::move (wx:snip% case)");
    x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::move (wx:snip% case)");

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Move(x0, x1, x2);

    
    
  } else  {
    float x0;
    float x1;

    
    if (n != 2) 
      scheme_wrong_count("wx:media-pasteboard%::move (without wx:snip% case)", 2, 2, n, p);
    x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::move (without wx:snip% case)");
    x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::move (without wx:snip% case)");

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Move(x0, x1);

    
    
  }

  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardMoveTo(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::move-to", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::move-to");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::move-to");

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->MoveTo(x0, x1, x2);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardRemove(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::remove", 0);

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Remove(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardErase(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Erase();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardDoPaste(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  long x0;

  
  x0 = objscheme_unbundle_integer(p[0], "wx:media-pasteboard%::do-paste");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::DoPaste(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->DoPaste(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardDoCopy(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  long x0;
  Bool x1;

  
  x0 = objscheme_unbundle_integer(p[0], "wx:media-pasteboard%::do-copy");
  x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::do-copy");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::DoCopy(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->DoCopy(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardDelete(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  if ((n >= 1) && objscheme_istype_wxSnip(p[0], NULL, 0)) {
    class wxSnip* x0;

    
    if (n != 1) 
      scheme_wrong_count("wx:media-pasteboard%::delete (wx:snip% case)", 1, 1, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::delete (wx:snip% case)", 0);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Delete(x0);

    
    
  } else  {

    
    if (n != 0) 
      scheme_wrong_count("wx:media-pasteboard%::delete (no argument case)", 0, 0, n, p);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Delete();

    
    
  }

  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardInsert(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  if ((n >= 2) && objscheme_istype_wxSnip(p[0], NULL, 0) && objscheme_istype_number(p[1], NULL)) {
    class wxSnip* x0;
    float x1;
    float x2;

    
    if (n != 3) 
      scheme_wrong_count("wx:media-pasteboard%::insert (wx:snip% with location case)", 3, 3, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::insert (wx:snip% with location case)", 0);
    x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::insert (wx:snip% with location case)");
    x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::insert (wx:snip% with location case)");

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Insert(x0, x1, x2);

    
    
  } else if ((n >= 3) && objscheme_istype_wxSnip(p[0], NULL, 0) && objscheme_istype_wxSnip(p[1], NULL, 1) && objscheme_istype_number(p[2], NULL)) {
    class wxSnip* x0;
    class wxSnip* x1;
    float x2;
    float x3;

    
    if (n != 4) 
      scheme_wrong_count("wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% and location case)", 4, 4, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% and location case)", 0);
    x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% and location case)", 1);
    x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% and location case)");
    x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% and location case)");

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Insert(x0, x1, x2, x3);

    
    
  } else if ((n >= 2) && objscheme_istype_wxSnip(p[0], NULL, 0) && objscheme_istype_wxSnip(p[1], NULL, 1)) {
    class wxSnip* x0;
    class wxSnip* x1;

    
    if (n != 2) 
      scheme_wrong_count("wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% case)", 2, 2, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% case)", 0);
    x1 = objscheme_unbundle_wxSnip(p[1], "wx:media-pasteboard%::insert (wx:snip% with before-wx:snip% case)", 1);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Insert(x0, x1);

    
    
  } else  {
    class wxSnip* x0;

    
    if (n != 1) 
      scheme_wrong_count("wx:media-pasteboard%::insert (wx:snip% without position case)", 1, 1, n, p);
    x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::insert (wx:snip% without position case)", 0);

    
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Insert(x0);

    
    
  }

  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetFlattenedText(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  string r;
  objscheme_check_valid(obj);
  long _x0;
  long* x0 = &_x0;

  

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetFlattenedText(x0);

  
  
  return scheme_make_sized_string(r, _x0, 0);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardPutFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  nstring r;
  objscheme_check_valid(obj);
  nstring x0;
  nstring x1;

  
  x0 = (nstring)objscheme_unbundle_nullable_string(p[0], "wx:media-pasteboard%::put-file");
  x1 = (nstring)objscheme_unbundle_nullable_string(p[1], "wx:media-pasteboard%::put-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::PutFile(x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->PutFile(x0, x1);

  
  
  return objscheme_bundle_string((char *)r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  nstring r;
  objscheme_check_valid(obj);
  nstring x0;

  
  x0 = (nstring)objscheme_unbundle_nullable_string(p[0], "wx:media-pasteboard%::get-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::GetFile(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetFile(x0);

  
  
  return objscheme_bundle_string((char *)r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterEditSequence(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterEditSequence();
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterEditSequence();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnEditSequence(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnEditSequence();
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnEditSequence();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterLoadFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::after-load-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterLoadFile(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterLoadFile(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnLoadFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  string x0;
  int x1;

  
  x0 = (string)objscheme_unbundle_string(p[0], "wx:media-pasteboard%::on-load-file");
  x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::on-load-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnLoadFile(x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnLoadFile(x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAfterSaveFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::after-save-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AfterSaveFile(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AfterSaveFile(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnSaveFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  string x0;
  int x1;

  
  x0 = (string)objscheme_unbundle_string(p[0], "wx:media-pasteboard%::on-save-file");
  x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::on-save-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnSaveFile(x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnSaveFile(x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnNewBox(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxSnip* r;
  objscheme_check_valid(obj);
  int x0;

  
  x0 = objscheme_unbundle_integer(p[0], "wx:media-pasteboard%::on-new-box");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnNewBox(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnNewBox(x0);

  
  
  return objscheme_bundle_wxSnip(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnNewImageSnip(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxImageSnip* r;
  objscheme_check_valid(obj);
  nstring x0;
  long x1;
  Bool x2;
  Bool x3;

  
  x0 = (nstring)objscheme_unbundle_nullable_string(p[0], "wx:media-pasteboard%::on-new-image-snip");
  x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::on-new-image-snip");
  x2 = objscheme_unbundle_bool(p[2], "wx:media-pasteboard%::on-new-image-snip");
  x3 = objscheme_unbundle_bool(p[3], "wx:media-pasteboard%::on-new-image-snip");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnNewImageSnip(x0, x1, x2, x3);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnNewImageSnip(x0, x1, x2, x3);

  
  
  return objscheme_bundle_wxImageSnip(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardInvalidateBitmapCache(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  float x0;
  float x1;
  float x2;
  float x3;

  
  if (n > 0) {
    x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::invalidate-bitmap-cache");
  } else
    x0 = 0.0;
  if (n > 1) {
    x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::invalidate-bitmap-cache");
  } else
    x1 = 0.0;
  if (n > 2) {
    x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::invalidate-bitmap-cache");
  } else
    x2 = -1.0;
  if (n > 3) {
    x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::invalidate-bitmap-cache");
  } else
    x3 = -1.0;

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::InvalidateBitmapCache(x0, x1, x2, x3);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->InvalidateBitmapCache(x0, x1, x2, x3);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnPaint(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;
  class wxDC* x1;
  float x2;
  float x3;
  float x4;
  float x5;
  float x6;
  float x7;
  int x8;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::on-paint");
  x1 = objscheme_unbundle_wxDC(p[1], "wx:media-pasteboard%::on-paint", 0);
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::on-paint");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::on-paint");
  x4 = objscheme_unbundle_float(p[4], "wx:media-pasteboard%::on-paint");
  x5 = objscheme_unbundle_float(p[5], "wx:media-pasteboard%::on-paint");
  x6 = objscheme_unbundle_float(p[6], "wx:media-pasteboard%::on-paint");
  x7 = objscheme_unbundle_float(p[7], "wx:media-pasteboard%::on-paint");
  x8 = objscheme_unbundle_integer(p[8], "wx:media-pasteboard%::on-paint");

  if (x1 && !x1->Ok()) scheme_signal_error("%s: bad bitmap", "wx:media-buffer%::on-paint");
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnPaint(x0, x1, x2, x3, x4, x5, x6, x7, x8);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnPaint(x0, x1, x2, x3, x4, x5, x6, x7, x8);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardWriteFootersToFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamOut* x0;

  
  x0 = objscheme_unbundle_wxMediaStreamOut(p[0], "wx:media-pasteboard%::write-footers-to-file", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::WriteFootersToFile(*x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->WriteFootersToFile(*x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardWriteHeadersToFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamOut* x0;

  
  x0 = objscheme_unbundle_wxMediaStreamOut(p[0], "wx:media-pasteboard%::write-headers-to-file", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::WriteHeadersToFile(*x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->WriteHeadersToFile(*x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardReadFooterFromFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamIn* x0;
  string x1;

  
  x0 = objscheme_unbundle_wxMediaStreamIn(p[0], "wx:media-pasteboard%::read-footer-from-file", 0);
  x1 = (string)objscheme_unbundle_string(p[1], "wx:media-pasteboard%::read-footer-from-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::ReadFooterFromFile(*x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ReadFooterFromFile(*x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardReadHeaderFromFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamIn* x0;
  string x1;

  
  x0 = objscheme_unbundle_wxMediaStreamIn(p[0], "wx:media-pasteboard%::read-header-from-file", 0);
  x1 = (string)objscheme_unbundle_string(p[1], "wx:media-pasteboard%::read-header-from-file");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::ReadHeaderFromFile(*x0, x1);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ReadHeaderFromFile(*x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardWriteToFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamOut* x0;

  
  x0 = objscheme_unbundle_wxMediaStreamOut(p[0], "wx:media-pasteboard%::write-to-file", 0);

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->WriteToFile(*x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardReadFromFile(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxMediaStreamIn* x0;
  Bool x1;

  
  x0 = objscheme_unbundle_wxMediaStreamIn(p[0], "wx:media-pasteboard%::read-from-file", 0);
  if (n > 1) {
    x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::read-from-file");
  } else
    x1 = FALSE;

  
  r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ReadFromFile(*x0, x1);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetFilename(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  string x0;
  Bool x1;

  
  x0 = (string)objscheme_unbundle_string(p[0], "wx:media-pasteboard%::set-filename");
  if (n > 1) {
    x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::set-filename");
  } else
    x1 = FALSE;

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::SetFilename(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetFilename(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardReleaseSnip(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::release-snip", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::ReleaseSnip(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ReleaseSnip(x0);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetModified(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::set-modified");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::SetModified(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetModified(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetSnipData(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  class wxBufferData* x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::set-snip-data", 0);
  x1 = objscheme_unbundle_wxBufferData(p[1], "wx:media-pasteboard%::set-snip-data", 1);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::SetSnipData(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetSnipData(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardGetSnipData(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxBufferData* r;
  objscheme_check_valid(obj);
  class wxSnip* x0;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::get-snip-data", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::GetSnipData(x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->GetSnipData(x0);

  
  
  return objscheme_bundle_wxBufferData(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardNeedsUpdate(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;
  float x3;
  float x4;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::needs-update", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::needs-update");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::needs-update");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::needs-update");
  x4 = objscheme_unbundle_float(p[4], "wx:media-pasteboard%::needs-update");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::NeedsUpdate(x0, x1, x2, x3, x4);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->NeedsUpdate(x0, x1, x2, x3, x4);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardResized(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  Bool x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::resized", 0);
  x1 = objscheme_unbundle_bool(p[1], "wx:media-pasteboard%::resized");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::Resized(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Resized(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSetCaretOwner(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxSnip* x0;
  int x1;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::set-caret-owner", 1);
  if (n > 1) {
    x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::set-caret-owner");
  } else
    x1 = wxFOCUS_IMMEDIATE;

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::SetCaretOwner(x0, x1);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SetCaretOwner(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardScrollTo(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  Bool r;
  objscheme_check_valid(obj);
  class wxSnip* x0;
  float x1;
  float x2;
  float x3;
  float x4;
  Bool x5;

  
  x0 = objscheme_unbundle_wxSnip(p[0], "wx:media-pasteboard%::scroll-to", 0);
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::scroll-to");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::scroll-to");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::scroll-to");
  x4 = objscheme_unbundle_float(p[4], "wx:media-pasteboard%::scroll-to");
  x5 = objscheme_unbundle_bool(p[5], "wx:media-pasteboard%::scroll-to");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::ScrollTo(x0, x1, x2, x3, x4, x5);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->ScrollTo(x0, x1, x2, x3, x4, x5);

  
  
  return (r ? scheme_true : scheme_false);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnChange(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnChange();
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnChange();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnFocus(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::on-focus");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnFocus(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnFocus(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnDefaultChar(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxKeyEvent* x0;

  
  x0 = objscheme_unbundle_wxKeyEvent(p[0], "wx:media-pasteboard%::on-default-char", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnDefaultChar(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnDefaultChar(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnDefaultEvent(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxMouseEvent* x0;

  
  x0 = objscheme_unbundle_wxMouseEvent(p[0], "wx:media-pasteboard%::on-default-event", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnDefaultEvent(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnDefaultEvent(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnLocalChar(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxKeyEvent* x0;

  
  x0 = objscheme_unbundle_wxKeyEvent(p[0], "wx:media-pasteboard%::on-local-char", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnLocalChar(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnLocalChar(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnLocalEvent(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxMouseEvent* x0;

  
  x0 = objscheme_unbundle_wxMouseEvent(p[0], "wx:media-pasteboard%::on-local-event", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnLocalEvent(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnLocalEvent(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardSizeCacheInvalid(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);

  

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::SizeCacheInvalid();
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->SizeCacheInvalid();

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOwnCaret(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;

  
  x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::own-caret");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OwnCaret(x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OwnCaret(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardRefresh(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  float x0;
  float x1;
  float x2;
  float x3;
  Bool x4;

  
  x0 = objscheme_unbundle_float(p[0], "wx:media-pasteboard%::refresh");
  x1 = objscheme_unbundle_float(p[1], "wx:media-pasteboard%::refresh");
  x2 = objscheme_unbundle_float(p[2], "wx:media-pasteboard%::refresh");
  x3 = objscheme_unbundle_float(p[3], "wx:media-pasteboard%::refresh");
  x4 = objscheme_unbundle_bool(p[4], "wx:media-pasteboard%::refresh");

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::Refresh(x0, x1, x2, x3, x4);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Refresh(x0, x1, x2, x3, x4);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardAdjustCursor(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  class wxCursor* r;
  objscheme_check_valid(obj);
  class wxMouseEvent* x0;

  
  x0 = objscheme_unbundle_wxMouseEvent(p[0], "wx:media-pasteboard%::adjust-cursor", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    r = ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::AdjustCursor(*x0);
  else
    r = ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->AdjustCursor(*x0);

  
  
  return objscheme_bundle_wxCursor(r);
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnChar(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxKeyEvent* x0;

  
  x0 = objscheme_unbundle_wxKeyEvent(p[0], "wx:media-pasteboard%::on-char", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnChar(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnChar(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardOnEvent(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  class wxMouseEvent* x0;

  
  x0 = objscheme_unbundle_wxMouseEvent(p[0], "wx:media-pasteboard%::on-event", 0);

  
  if (((Scheme_Class_Object *)obj)->primflag)
    ((os_wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->wxMediaPasteboard::OnEvent(*x0);
  else
    ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->OnEvent(*x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardKill(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  long x0;

  
  if (n > 0) {
    x0 = objscheme_unbundle_integer(p[0], "wx:media-pasteboard%::kill");
  } else
    x0 = 0;

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Kill(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardPaste(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  long x0;

  
  if (n > 0) {
    x0 = objscheme_unbundle_integer(p[0], "wx:media-pasteboard%::paste");
  } else
    x0 = 0;

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Paste(x0);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardCopy(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;
  long x1;

  
  if (n > 0) {
    x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::copy");
  } else
    x0 = FALSE;
  if (n > 1) {
    x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::copy");
  } else
    x1 = 0;

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Copy(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboardCut(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(n) WXS_USE_ARGUMENT(p)
  objscheme_check_valid(obj);
  Bool x0;
  long x1;

  
  if (n > 0) {
    x0 = objscheme_unbundle_bool(p[0], "wx:media-pasteboard%::cut");
  } else
    x0 = FALSE;
  if (n > 1) {
    x1 = objscheme_unbundle_integer(p[1], "wx:media-pasteboard%::cut");
  } else
    x1 = 0;

  
  ((wxMediaPasteboard *)((Scheme_Class_Object *)obj)->primdata)->Cut(x0, x1);

  
  
  return scheme_void;
}

#pragma argsused
static Scheme_Object *os_wxMediaPasteboard_ConstructScheme(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
  os_wxMediaPasteboard *realobj;

  
  if (n != 0) 
    scheme_wrong_count("wx:media-pasteboard%::initialization", 0, 0, n, p);

  
  realobj = new os_wxMediaPasteboard(obj);
  
  
  ((Scheme_Class_Object *)obj)->primdata = realobj;
  objscheme_register_primpointer(&((Scheme_Class_Object *)obj)->primdata);
  ((Scheme_Class_Object *)obj)->primflag = 1;
  return obj;
}

static Scheme_Object *objscheme_classname_os_wxMediaPasteboard(Scheme_Object *obj, int n,  Scheme_Object *p[])
{
 WXS_USE_ARGUMENT(obj);
  if (n) scheme_wrong_count("wx:media-pasteboard%" "::get-class-name", 0, 0, n, p);
  return scheme_intern_symbol("wx:media-pasteboard%");
}

void objscheme_setup_wxMediaPasteboard(void *env)
{
if (os_wxMediaPasteboard_class) {
    objscheme_add_global_class(os_wxMediaPasteboard_class,  "wx:media-pasteboard%", env);
} else {
  os_wxMediaPasteboard_class = objscheme_def_prim_class(env, "wx:media-pasteboard%", "wx:media-buffer%", os_wxMediaPasteboard_ConstructScheme, 93);

  scheme_add_method_w_arity(os_wxMediaPasteboard_class,"get-class-name",objscheme_classname_os_wxMediaPasteboard, 0, 0);

 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-scroll-step", os_wxMediaPasteboardSetScrollStep, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-scroll-step", os_wxMediaPasteboardGetScrollStep, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-selection-visible", os_wxMediaPasteboardSetSelectionVisible, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-selection-visible", os_wxMediaPasteboardGetSelectionVisible, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-dragable", os_wxMediaPasteboardSetDragable, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-dragable", os_wxMediaPasteboardGetDragable, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-interactive-resize", os_wxMediaPasteboardAfterInteractiveResize, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-interactive-resize", os_wxMediaPasteboardOnInteractiveResize, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-interactive-move", os_wxMediaPasteboardAfterInteractiveMove, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-interactive-move", os_wxMediaPasteboardOnInteractiveMove, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "interactive-adjust-resize", os_wxMediaPasteboardInteractiveAdjustResize, 3, 3);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "interactive-adjust-move", os_wxMediaPasteboardInteractiveAdjustMove, 3, 3);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "interactive-adjust-mouse", os_wxMediaPasteboardInteractiveAdjustMouse, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-double-click", os_wxMediaPasteboardOnDoubleClick, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-select", os_wxMediaPasteboardAfterSelect, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-select", os_wxMediaPasteboardOnSelect, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-resize", os_wxMediaPasteboardAfterResize, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-resize", os_wxMediaPasteboardOnResize, 3, 3);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-move-to", os_wxMediaPasteboardAfterMoveTo, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-move-to", os_wxMediaPasteboardOnMoveTo, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-delete", os_wxMediaPasteboardAfterDelete, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-delete", os_wxMediaPasteboardOnDelete, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-insert", os_wxMediaPasteboardAfterInsert, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-insert", os_wxMediaPasteboardOnInsert, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "find-next-selected-snip", os_wxMediaPasteboardFindNextSelectedSnip, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "is-selected?", os_wxMediaPasteboardIsSelected, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "find-first-snip", os_wxMediaPasteboardFindFirstSnip, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "find-snip", os_wxMediaPasteboardFindSnip, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "add-pasteboard-functions", os_wxMediaPasteboardAddPasteboardFunctions, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-center", os_wxMediaPasteboardGetCenter, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "remove-selected", os_wxMediaPasteboardRemoveSelected, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "no-selected", os_wxMediaPasteboardNoSelected, 0, 0);
 scheme_add_method(os_wxMediaPasteboard_class, "add-selected", os_wxMediaPasteboardAddSelected);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-selected", os_wxMediaPasteboardSetSelected, 1, 1);
 scheme_add_method(os_wxMediaPasteboard_class, "change-style", os_wxMediaPasteboardChangeStyle);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-after", os_wxMediaPasteboardSetAfter, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-before", os_wxMediaPasteboardSetBefore, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "lower", os_wxMediaPasteboardLower, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "raise", os_wxMediaPasteboardRaise, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "resize", os_wxMediaPasteboardResize, 3, 3);
 scheme_add_method(os_wxMediaPasteboard_class, "move", os_wxMediaPasteboardMove);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "move-to", os_wxMediaPasteboardMoveTo, 3, 3);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "remove", os_wxMediaPasteboardRemove, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "erase", os_wxMediaPasteboardErase, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "do-paste", os_wxMediaPasteboardDoPaste, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "do-copy", os_wxMediaPasteboardDoCopy, 2, 2);
 scheme_add_method(os_wxMediaPasteboard_class, "delete", os_wxMediaPasteboardDelete);
 scheme_add_method(os_wxMediaPasteboard_class, "insert", os_wxMediaPasteboardInsert);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-flattened-text", os_wxMediaPasteboardGetFlattenedText, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "put-file", os_wxMediaPasteboardPutFile, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-file", os_wxMediaPasteboardGetFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-edit-sequence", os_wxMediaPasteboardAfterEditSequence, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-edit-sequence", os_wxMediaPasteboardOnEditSequence, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-load-file", os_wxMediaPasteboardAfterLoadFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-load-file", os_wxMediaPasteboardOnLoadFile, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "after-save-file", os_wxMediaPasteboardAfterSaveFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-save-file", os_wxMediaPasteboardOnSaveFile, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-new-box", os_wxMediaPasteboardOnNewBox, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-new-image-snip", os_wxMediaPasteboardOnNewImageSnip, 4, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "invalidate-bitmap-cache", os_wxMediaPasteboardInvalidateBitmapCache, 0, 4);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-paint", os_wxMediaPasteboardOnPaint, 9, 9);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "write-footers-to-file", os_wxMediaPasteboardWriteFootersToFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "write-headers-to-file", os_wxMediaPasteboardWriteHeadersToFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "read-footer-from-file", os_wxMediaPasteboardReadFooterFromFile, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "read-header-from-file", os_wxMediaPasteboardReadHeaderFromFile, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "write-to-file", os_wxMediaPasteboardWriteToFile, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "read-from-file", os_wxMediaPasteboardReadFromFile, 1, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-filename", os_wxMediaPasteboardSetFilename, 1, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "release-snip", os_wxMediaPasteboardReleaseSnip, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-modified", os_wxMediaPasteboardSetModified, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-snip-data", os_wxMediaPasteboardSetSnipData, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "get-snip-data", os_wxMediaPasteboardGetSnipData, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "needs-update", os_wxMediaPasteboardNeedsUpdate, 5, 5);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "resized", os_wxMediaPasteboardResized, 2, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "set-caret-owner", os_wxMediaPasteboardSetCaretOwner, 1, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "scroll-to", os_wxMediaPasteboardScrollTo, 6, 6);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-change", os_wxMediaPasteboardOnChange, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-focus", os_wxMediaPasteboardOnFocus, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-default-char", os_wxMediaPasteboardOnDefaultChar, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-default-event", os_wxMediaPasteboardOnDefaultEvent, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-local-char", os_wxMediaPasteboardOnLocalChar, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-local-event", os_wxMediaPasteboardOnLocalEvent, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "size-cache-invalid", os_wxMediaPasteboardSizeCacheInvalid, 0, 0);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "own-caret", os_wxMediaPasteboardOwnCaret, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "refresh", os_wxMediaPasteboardRefresh, 5, 5);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "adjust-cursor", os_wxMediaPasteboardAdjustCursor, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-char", os_wxMediaPasteboardOnChar, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "on-event", os_wxMediaPasteboardOnEvent, 1, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "kill", os_wxMediaPasteboardKill, 0, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "paste", os_wxMediaPasteboardPaste, 0, 1);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "copy", os_wxMediaPasteboardCopy, 0, 2);
 scheme_add_method_w_arity(os_wxMediaPasteboard_class, "cut", os_wxMediaPasteboardCut, 0, 2);


  scheme_made_class(os_wxMediaPasteboard_class);

  objscheme_install_bundler((Objscheme_Bundler)objscheme_bundle_wxMediaPasteboard, wxTYPE_MEDIA_PASTEBOARD);

}
}

int objscheme_istype_wxMediaPasteboard(Scheme_Object *obj, const char *stop, int nullOK)
{
  if (nullOK && SCHEME_NULLP(obj)) return 1;
  if (SAME_TYPE(SCHEME_TYPE(obj), scheme_object_type)
      && scheme_is_subclass(((Scheme_Class_Object *)obj)->sclass,          os_wxMediaPasteboard_class))
    return 1;
  else {
    if (!stop)
       return 0;
    scheme_wrong_type(stop, "wx:media-pasteboard%", -1, 0, &obj);
    return 0;
  }
}

Scheme_Object *objscheme_bundle_wxMediaPasteboard(class wxMediaPasteboard *realobj)
{
  Scheme_Class_Object *obj;
  Scheme_Object *sobj;

  if (!realobj) return scheme_null;

  if (realobj->__gc_external)
    return (Scheme_Object *)realobj->__gc_external;
  if ((realobj->__type != wxTYPE_MEDIA_PASTEBOARD) && (sobj = objscheme_bundle_by_type(realobj, realobj->__type)))
    return sobj;
  obj = (Scheme_Class_Object *)scheme_make_uninited_object(os_wxMediaPasteboard_class);

  obj->primdata = realobj;
  objscheme_register_primpointer(&obj->primdata);
  obj->primflag = 0;

  realobj->__gc_external = (void *)obj;
  objscheme_backpointer(&realobj->__gc_external);
  return (Scheme_Object *)obj;
}

class wxMediaPasteboard *objscheme_unbundle_wxMediaPasteboard(Scheme_Object *obj, const char *where, int nullOK)
{
  if (nullOK && SCHEME_NULLP(obj)) return NULL;

  (void)objscheme_istype_wxMediaPasteboard(obj, where, nullOK);
  Scheme_Class_Object *o = (Scheme_Class_Object *)obj;
  objscheme_check_valid(obj);
  if (o->primflag)
    return (os_wxMediaPasteboard *)o->primdata;
  else
    return (wxMediaPasteboard *)o->primdata;
}

