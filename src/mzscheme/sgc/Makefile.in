#
# Makefile for SenoraGC
#

srcdir = @srcdir@

CC = @CC@
CXX = @CXX@
RANLIB = @RANLIB@
AR = @AR@
ARFLAGS = @ARFLAGS@

CFLAGS = @CFLAGS@ @OPTIONS@

OBJS =  sgc.o

SRCS =  $(srcdir)/sgc.c

gc.a: $(OBJS)
	$(MAKE) gcobjects
	$(AR) $(ARFLAGS) gc.a $(OBJS)
	$(RANLIB) gc.a

test: $(OBJS) test.o
	$(CC) -o test $(OBJS) test.o

gcobjects: $(OBJS)

sgc.o: $(srcdir)/sgc.c $(srcdir)/autostat.inc $(srcdir)/collect.inc $(srcdir)/splay.c
	$(CC) $(CFLAGS) -c $(srcdir)/sgc.c -o sgc.o

test.o: $(srcdir)/test.c
	$(CC) $(CFLAGS) -c $(srcdir)/test.c -o test.o

clean:
	/bin/rm -f $(OBJS) gc.a test

# Extra dependencies

sgc.o: $(srcdir)/sgc.h

test.o: $(srcdir)/sgc.h
